# secrets
.env
.env.*
!.env.example

# build
node_modules/
dist/
.astro/

# terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
infra/.build/

# os
.DS_Store

# Bundled into the waiver deploy zip by terraform, not committed.
infra/lambda-waiver/node_modules/
infra/lambda-waiver/waiver.json
infra/lambda-waiver/logo.png
